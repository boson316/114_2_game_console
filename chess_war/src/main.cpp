#include "Game.h"
#include <memory>

// ============================================================================
// main: 遊戲程式進入點 (最簡化進入點)
// ============================================================================
int main() {
    // 使用 std::unique_ptr 進行安全記憶體生命週期管理
    std::unique_ptr<Game> game = std::make_unique<Game>();
    
    // 啟動遊戲主迴圈
    game->run();
    
    return 0;
}
